generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model gyerek {
  id  Int    @id @default(autoincrement())
  nev String
  lakcim String
  alapot Boolean
  jatek String
}
